import { memo } from "react";

const AddedQuize = ({ quize }) => {
    return (
        <div className="bg-white p-4 rounded-md shadow-md">

        </div>
    );
};

export default memo(AddedQuize);  